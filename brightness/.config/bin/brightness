#!/bin/sh

if [ $# == 0 ]
then
    echo brightness is `cat /sys/class/backlight/radeon_bl0/brightness`
    exit
fi
if [ `whoami` != 'root' -a $# > 0 ]
then
    echo must be root
    exit
fi
echo $1 > /sys/class/backlight/radeon_bl0/brightness
echo set brightness to $1
